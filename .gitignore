node_modules/
release/
dist/
frontend/node_modules/
frontend/dist/
assets/

# Build artifacts
*.zip
QuickLauncher-Final/
QuickLauncher-TabBridge.zip
QuickLauncher_1.0.0.zip
quick-launcher.zip

# Dev helpers
make-icon-main.js
make-icon.html

# OS
.DS_Store
Thumbs.db

# Local log files (user copies main.log here for analysis sessions)
main.log
*.log

# Test artifacts
playwright-report/
test-results/
coverage/

# Claude
.claude/
PROJECT_NOTES.md
walkthrough.md
설명서.txt

# Secrets — never commit. Env files and any token-bearing helpers.
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local
frontend/.env.*.local
# Top-level ps1 helpers are typically one-off scripts that contain
# secrets (the historical create_release.ps1 / upload_assets.ps1 had
# hardcoded GH tokens). The PS scripts the app actually ships live in
# ps-scripts/ and chrome-extension/ — those are NOT blocked.
/*.ps1

# v1.3.50 — repo cleanup
# 사용자 백업 .nost 파일 (수동 export / 테스트용)
*.nost
# 별도 프로젝트 (Next.js landing page) — 다른 repo 로 옮기거나 별도 관리
site/
# 다른 프로젝트 (제안서 분석) 의 임시 텍스트 — repo 와 무관
모집단 조사*.txt
